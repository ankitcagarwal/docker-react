sudo: required
services:
    - docker

before_install:
    - docker build -t ankitcagarwal/docker-react -f Dockerfile.dev .

script:
    - docker run ankitcagarwal/docker-react npm run test -- --coverage